[pytest]
testpaths = tests
python_files = test_*.py
markers =
    live: requires real API key and makes live LLM calls
    slow: long-running tests
    smoke: starts adk web, api_server, or UI to verify no import errors
addopts = -v --tb=short
